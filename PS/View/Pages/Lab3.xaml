<UserControl x:Class="PS.View.Pages.Lab3"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <TextBlock HorizontalAlignment="Center">Lab3 - SMTP</TextBlock>
        <StackPanel Orientation="Vertical" Margin="20">
            <TextBox controls:TextBoxHelper.Watermark="Adresat" controls:TextBoxHelper.UseFloatingWatermark="True" Text="{Binding MailFrom}"/>

            <StackPanel Orientation="Horizontal" Margin="0,20" HorizontalAlignment="Center">
                <Button Content="Wyślij wiadomość" IsEnabled="{Binding SendButtonState}" Command="{Binding SendCommand}" Width="200"/>
            </StackPanel>
        </StackPanel>
        
        <controls:ProgressRing IsActive="{Binding IsLoading}"/>
    </Grid>
</UserControl>
